# What we use for Technical Docs
We use `MDX` file for the documentation. This means you can use `React Components` `html` but mostly you just write the Docs in [Markup Down](https://www.markdownguide.org/basic-syntax/).

## Components
We use some built in components like the `Code Block`. 