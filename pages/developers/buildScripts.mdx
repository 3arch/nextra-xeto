# Xeto, TypeScript, and Metadata

Here we will show you a list of build commands like
-AST tree
-Validation


##Node Command Lines and NPM


##Fantom Builds with Haxall
-Ast

### Run an Axon Script
Validate VAVs against the `G36ReheatVav`
`validate-filtered.axon https://project-haystack.org/example/download/bravo.zinc vav ashrae.g36::G36ReheatVav`

import { Callout, Steps } from 'nextra-theme-docs'

<Steps>
### Step-1
asdfasdf
asdfasdfasdf
asdfasdfsdfasfd
### Another Step
```js {2} filename="next.config.js"
const withNextra = require('nextra')({
  theme: './theme.tsx',
})

module.exports = withNextra({
  // Other Next.js configurations
  ...
})
```

### Create a Basic Theme

You can now start working on your theme! In your root directory, create the corresponding `theme.tsx` file with basic content:

```tsx filename="theme.tsx" /children/
import type { NextraThemeLayoutProps } from 'nextra'

export default function Layout({ children }: NextraThemeLayoutProps) {
  return (
    <div>
      <h1>My Theme</h1>
      <div style={{ border: '1px solid' }}>{children}</div>
    </div>
  )
}
```

</Steps>
